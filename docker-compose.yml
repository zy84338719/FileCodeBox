version: '3.8'

services:
  filecodebox-go:
    build: .
    ports:
      - "12345:12345"
    volumes:
      - ./data:/app/data
      - ./themes:/app/themes
    environment:
      - GIN_MODE=release
    restart: unless-stopped
